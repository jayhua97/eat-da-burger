<h1>Burgers!</h1>

<img src="https://ih1.redbubble.net/image.568550998.2774/flat,550x550,075,f.u1.jpg" width="200px" height="200px">

<div class="wrapper">
<div class="notEaten col-md-6">
<h2>Burgers that haven't been eaten!</h2>

<ol>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ol>
</div>

<div class="eaten col-md-6">
<h2>Burgers that have been eaten!</h2>

<ol>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger-block devour=false}}
    {{/if}}
  {{/each}}
</ol>
</div>
</div>

<div class="createBurger">
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>
</div>
